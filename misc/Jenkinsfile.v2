pipeline {
    agent any
    stages {
        stage('test') {
            agent {
                docker {
                    image 'node:16'
               
                }
            }
            steps {
                sh 'npm install --only=dev'
                sh 'npm test'
            }
        }
    }
}
