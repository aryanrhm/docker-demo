pipeline {
    agent any
    stages {
        stage('test') {
            agent {
                docker {
                    image 'node:16'
                    args '-u root:root'            
                }
            }
            steps {
                sh 'npm install --only=dev'
                sh 'npm test'
            }
        }
    }
}
