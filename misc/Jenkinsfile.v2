pipeline {
    agent {
        docker { image 'node:16' }
    }
    stages {
        stage('test') {
            steps {
                sh 'npm install --only=dev'
                sh 'npm test'
            }
        }
    }
}
