pipeline {
    agent any
    stages {
        stage('test') {
            agent {
                docker {
                    image 'node:16'
                    args '-u 1000:1000'            
                }
            }
            steps {
                sh 'npm install --only=dev'
                sh 'npm test'
            }
        }
    }
}
