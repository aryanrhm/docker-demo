pipeline {
    agent {
        docker { image 'node:16' 
                 args '-u root'
        }
    }
    stages {
        stage('test') {
            steps {
                sh 'npm install --only=dev'
                sh 'npm test'
            }
        }
    }
}
